package demo;

import org.example.Recursive;

public class used {
    public static void main(String[] args) {
        Recursive  r = new Recursive();


       // org.example.Recursive r = new org.example.Recursive();
        r.validate(10);
        r.sumOftwovalues(10,30);
    }
}
